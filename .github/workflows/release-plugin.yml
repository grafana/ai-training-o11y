name: Release
on:
  push:
    tags:
      - "v*" # Run workflow on version tags, e.g. v1.0.0.

permissions:
  contents: write

jobs:
  release:
    uses: sandersaarond/ai-training-o11y/.github/workflows/build-plugin.yml@build-plugin
    with:
      policy_token: ${{ secrets.GRAFANA_ACCESS_POLICY_TOKEN }}
